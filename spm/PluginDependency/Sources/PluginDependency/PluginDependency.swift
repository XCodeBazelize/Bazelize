/// Neither `stamp` nor `mark` is here: one comes from the plugin of the package
/// next door, the other from a package that is nothing but a plugin.
public func stampedValue() -> String {
    stamp
}

public func markedValue() -> String {
    mark
}
