#pragma once

/// Included by the shader beside it. A header is not a resource, but a shader
/// that includes one has to be compiled with it there, so it joins the group
/// the shader is in.
#define SHADER_FACTOR 2.0
